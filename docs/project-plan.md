# BiliLini 直播弹幕分析工具 — 项目计划书

## 一、项目概述

### 1.1 项目背景

B 站直播已成为国内主流直播平台之一，直播弹幕承载着观众情绪、讨论热点和互动行为等丰富信息。目前缺乏一款面向个人用户、兼具实时分析和离线深度分析能力的弹幕分析工具。本项目旨在填补这一空白。

### 1.2 项目目标

构建一款基于 Python + React 的桌面级 B 站直播弹幕采集与分析工具，实现以下核心目标：

- **实时采集**：通过 WebSocket 连接 B 站弹幕服务器，稳定获取直播间弹幕数据
- **实时分析**：直播过程中提供弹幕频率、关键词、情感等维度的即时分析
- **深度分析**：支持对历史弹幕数据进行离线的多维度深度分析
- **可视化展示**：通过直观的 Web 仪表盘呈现分析结果

### 1.3 项目范围

| 维度 | 范围内 | 范围外 |
|------|--------|--------|
| 使用场景 | 个人单机使用 | 多人协作、SaaS 化部署 |
| 目标平台 | Windows / macOS / Linux | 移动端 App |
| 监控规模 | 1-5 个直播间并发 | 大规模（50+）直播间 |
| 分析语言 | 中文弹幕 | 其他语言弹幕 |
| 数据存储 | 本地 SQLite | 云存储、分布式数据库 |

---

## 二、项目组织

### 2.1 角色与职责

| 角色 | 职责 |
|------|------|
| 项目负责人 | 整体架构决策、技术选型、进度把控 |
| 后端开发 | 弹幕采集模块、协议解析、API 服务、数据库 |
| 前端开发 | React 页面开发、图表集成、WebSocket 对接 |
| 算法开发 | 情感分析、关键词提取、热点检测、用户画像 |
| 测试 | 功能测试、性能测试、协议兼容性验证 |

### 2.2 协作工具

| 类别 | 工具 |
|------|------|
| 代码仓库 | Git + GitHub |
| 任务管理 | GitHub Issues / Projects |
| 文档管理 | Markdown (项目仓库内) |
| 开发环境 | VS Code + Node.js 20+ |

---

## 三、项目阶段规划

本项目采用**增量迭代**开发模式，分为四个阶段。

```
P0 基础采集          P1 实时展示          P2 实时分析          P3 深度分析          P4 完善打磨
   2周                 3周                 3周                 3周                 2周
   │                   │                   │                   │                   │
   ▼                   ▼                   ▼                   ▼                   ▼
┌────────┐        ┌────────┐        ┌────────┐        ┌────────┐        ┌────────┐
│ 采集   │   →    │ API    │   →    │ 分析   │   →    │ 深度   │   →    │ 稳定   │
│ 存储   │        │ 前端   │        │ 图表   │        │ 报告   │        │ 性能   │
└────────┘        └────────┘        └────────┘        └────────┘        └────────┘
```

### 3.1 P0 — 基础采集（核心 MVP 前置）✅ 已完成

**目标**：打通弹幕数据从 B 站服务器到本地数据库的全链路。

**时间预估**：2 周

| 任务编号 | 任务描述 | 优先级 | 状态 | 产出 |
|---------|---------|--------|------|------|
| P0-1 | B 站弹幕二进制协议解析器 `parser.py` | P0 | ✅ | 支持 JSON/zlib/brotli 三种编码，brotli 递归解包 |
| P0-2 | 单直播间 WebSocket 客户端 `bili_client.py` | P0 | ✅ | 含 WBI 签名、buvid3 缓存、认证判定 |
| P0-3 | 心跳维持与断线重连机制 | P0 | ✅ | 25s 心跳 + 指数退避重连（4s~30s） |
| P0-4 | 弹幕数据解析为结构化对象 | P0 | ✅ | DanmuRecord + extractDanmuRecord() |
| P0-5 | SQLite 数据库初始化与建表 | P0 | ✅ | WAL 模式，rooms/danmu_records 表 |
| P0-6 | 弹幕写入存储（单条 + 批量） | P0 | ✅ | DanmuWriter 批量写入（100条/批） |
| P0-7 | CLI 启动脚本，验证端到端链路 | P0 | ✅ | `run.py` 可运行 |

**里程碑 M0 — 采集链路打通 ✅**：通过命令行成功连接一个 B 站直播间，持续采集弹幕并存入 SQLite。

---

### 3.2 P1 — 实时展示 ✅ 已完成

**目标**：搭建前后端服务框架，实现弹幕的实时 Web 展示。

**时间预估**：3 周

| 任务编号 | 任务描述 | 优先级 | 状态 | 产出 |
|---------|---------|--------|------|------|
| P1-1 | FastAPI 服务骨架 + CORS + 异常处理 | P1 | ✅ 已完成 | `start_server.py` + `server/main.py` |
| P1-2 | 直播间管理 API（CRUD + 采集启停） | P1 | ✅ 已完成 | `/api/rooms/*` |
| P1-3 | 弹幕查询 API（分页、统计） | P1 | ✅ 已完成 | `/api/danmu/*` |
| P1-4 | WebSocket 推送服务搭建 | P1 | ✅ 已完成 | `/ws` 端点，支持 subscribe/unsubscribe 消息 |
| P1-5 | 多直播间采集管理器 `manager.py` | P1 | ✅ 已完成 | 连接池管理、回调注入、状态同步 |
| P1-6 | 前端工程初始化（React + Vite + TailwindCSS + 路由） | P1 | ✅ 已完成 | 前端骨架、代理配置、路径别名 |
| P1-7 | 侧边栏 + 直播间列表组件 | P1 | ✅ 已完成 | Sidebar, RoomList, RoomItem, AddRoomModal |
| P1-8 | 弹幕墙组件（固定高度滚动） | P1 | ✅ 已完成 | RoomDetail, useDanmaku Hook, 节流渲染, 实时/历史弹幕区分 |
| P1-9 | 前端 WebSocket 连接 + 弹幕实时渲染 | P1 | ✅ 已完成 | WebSocket 自动重连、实时弹幕推送、新弹幕提示、自动滚动 |
| P1-10 | 添加直播间弹窗 + 采集启停控制 | P1 | ✅ 已完成 | 输入验证、状态同步、操作反馈、采集会话管理 |
| P1-11 | 采集历史卡片组件 | P1 | ✅ 已完成 | HistoryCard, 采集会话列表、时间标注、弹幕统计 |
| P1-12 | 渲染性能优化 | P1 | ✅ 已完成 | React.memo、roomsEqual 深比较、渲染节流 |

**里程碑 M1 — 实时弹幕墙可用 ✅**：Web 界面添加直播间后，可看到弹幕实时滚动展示，支持历史弹幕加载、实时/历史弹幕区分显示、采集历史卡片、新弹幕提示、自动滚动等完整弹幕交互体验。

---

### 3.3 P2 — 实时分析（MVP 完整体验）✅ 已完成

**目标**：在实时采集的基础上，增加多维度的即时分析能力。

**时间预估**：3 周

| 任务编号 | 任务描述 | 优先级 | 预估工时 | 产出 | 状态 |
|---------|---------|--------|---------|------|------|
| P2-1 | 停用词库整理 + jieba 集成 | P2 | 1d | `segment_engine.py` + 停用词库 + 自定义词典 | ✅ |
| P2-2 | 情感词典整理 + SnowNLP 情感打分引擎 | P2 | 2d | `sentiment_engine.py` + 训练数据 + 滑动窗口统计 | ✅ |
| P2-3 | 滑动窗口频率统计算法 | P2 | 1d | `frequency_engine.py` | ✅ |
| P2-4 | 关键词 Top K 增量更新算法 | P2 | 2d | `keyword_engine.py` | ✅ |
| P2-5 | 实时分析引擎整合 `realtime_engine.py` | P2 | 1d | `RealtimeAnalyzer` 解耦整合四个引擎 | ✅ |
| P2-6 | 分析数据 WebSocket 推送通道 | P2 | 1d | 数据推送 | ✅ |
| P2-7 | ECharts 集成 + 弹幕频率折线图 | P2 | 1d | FrequencyChart | ✅ |
| P2-8 | 情感占比饼图 | P2 | 1d | SentimentPieChart | ✅ |
| P2-9 | 关键词词云（echarts-wordcloud） | P2 | 2d | KeywordCloud | ✅ |
| P2-10 | 直播间详情页布局整合 | P2 | 1d | RoomDetail 页面重构 | ✅ |

**里程碑 M2 — MVP 完成 ✅**：完整可用，能实时采集、展示和分析弹幕，具备四维分析看板。

---

#### 3.3.1 关键技术设计

##### 后端分析引擎架构

采用**注册模式**实现各分析引擎的解耦，通过 `RealtimeAnalyzer` 统一管理和聚合。

```
RealtimeAnalyzer (核心协调器)
    │
    ├── SegmentEngine     (分词引擎)
    │       ├── jieba 分词
    │       ├── 哈工大停用词表过滤
    │       ├── 自定义词典支持
    │       └── 100ms 节流处理
    │
    ├── SentimentEngine   (情感分析引擎)
    │       ├── SnowNLP 情感打分
    │       ├── 正面/负面/中性三分类
    │       └── 60s 滑动窗口统计
    │
    ├── FrequencyEngine   (频率统计引擎)
    │       └── 10s 滑动窗口统计
    │
    └── KeywordEngine     (关键词引擎)
            ├── Top K 增量更新
            └── 最小堆优化
```

**核心设计原则**：
- **解耦**：各引擎独立实现，通过注册模式接入
- **实时**：100ms 节流处理，保证实时性
- **滑动窗口**：基于时间窗口的统计，避免数据无限增长

##### 前端布局设计

采用**低耦合组件架构**，实现职责分离和可复用性。

```
RoomDetail (页面容器 — 状态管理中心)
    │
    ├── RoomHeader        (纯展示组件)
    │       ├── 直播间信息展示
    │       ├── 采集控制按钮
    │       └── 操作反馈显示
    │
    ├── CollapsiblePanel  (通用折叠面板)
    │       └── HistoryCard (compact模式)
    │
    ├── DanmakuContainer  (容器组件)
    │       ├── DanmakuItem (纯展示)
    │       ├── 实时/历史弹幕区分
    │       ├── 新弹幕提示
    │       └── 自动滚动控制
    │
    └── AnalysisPanel     (容器组件)
            ├── FrequencyChart    (频率折线图)
            ├── SentimentPieChart (情感饼图)
            └── KeywordCloud      (关键词词云)
```

**布局优化方案**：
- **顶部区域**：标题栏 + 可折叠采集历史（默认收起）
- **主区域**：左侧弹幕墙（固定高度 500px，flex-1）+ 右侧分析面板（480px）
- **右侧面板**：1+2 布局 — 频率折线图全宽在上（180px），情感饼图和关键词词云并排在下（各 200px）

**低耦合设计原则**：
1. **单一职责**：每个组件只做一件事
2. **接口隔离**：通过 props 和 callbacks 进行通信，不直接引用
3. **依赖倒置**：组件依赖抽象类型，不依赖具体实现
4. **无状态优先**：纯展示组件不维护业务状态

##### 数据流设计

```
后端数据流：
    弹幕采集 → SegmentEngine → SentimentEngine → RealtimeAnalyzer → WebSocket推送
                          ↓                ↓
                   FrequencyEngine    KeywordEngine

前端数据流：
    WebSocketContext → RoomDetail (状态管理) → props → 子组件
    子组件 → callbacks → RoomDetail (事件处理)
```

---

#### 3.3.2 技术变更说明

| 变更项 | 原计划 | 实际方案 | 原因 |
|--------|--------|---------|------|
| 分词 | nodejieba（前端） | Python jieba（后端） | 更适合实时分析场景，避免前端性能问题 |
| 情感分析 | 自建词典 | SnowNLP | 成熟模型，支持自定义训练数据微调 |
| 架构 | 耦合式集成 | 注册模式解耦 | 通过 `RealtimeAnalyzer` 统一管理，便于扩展 |
| 布局 | 垂直堆叠 | 左右分栏 + 1+2布局 | 提升信息密度，三个图表同屏可见 |

---

#### 3.3.3 实现步骤详解

##### P2-1 分词引擎实现步骤

1. **安装依赖**：`pip install jieba`
2. **停用词库**：引入哈工大停用词表，过滤无意义词汇
3. **自定义词典**：支持加载 `custom_dict.txt` 扩展领域词汇
4. **节流处理**：100ms 批量处理，避免频繁分词调用
5. **接口设计**：`SegmentEngine.process(danmaku_list)` 返回分词结果

##### P2-2 情感分析实现步骤

1. **安装依赖**：`pip install snownlp`
2. **情感打分**：使用 SnowNLP 对每条弹幕进行情感评分（0-1）
3. **三分类**：评分 > 0.6 正面，< 0.4 负面，其余中性
4. **滑动窗口**：60s 时间窗口，维护正面/负面/中性计数
5. **接口设计**：`SentimentEngine.process(danmaku_list)` 返回实时情感统计

##### P2-3 频率统计实现步骤

1. **滑动窗口**：10s 时间窗口，维护时间序列数据
2. **计数更新**：每条弹幕加入时更新当前窗口计数
3. **窗口滚动**：超过窗口范围的数据自动移除
4. **接口设计**：`FrequencyEngine.process(danmaku_list)` 返回实时频率

##### P2-4 关键词提取实现步骤

1. **词频统计**：基于分词结果统计词频
2. **Top K 算法**：使用最小堆实现 Top K 增量更新
3. **频率计算**：计算每个关键词的频率占比
4. **接口设计**：`KeywordEngine.process(danmaku_list)` 返回 Top K 关键词

##### P2-5 实时分析引擎整合步骤

1. **注册模式**：各引擎通过 `@register_engine` 装饰器注册
2. **统一管理**：`RealtimeAnalyzer` 维护已注册引擎列表
3. **数据聚合**：调用所有引擎处理弹幕，聚合分析结果
4. **数据推送**：通过 WebSocket 向订阅者推送分析数据

##### P2-6 WebSocket 数据推送步骤

1. **消息类型扩展**：新增 `stats` 消息类型
2. **订阅管理**：支持订阅特定房间的分析数据
3. **线程安全**：使用 `asyncio.run_coroutine_threadsafe` 实现线程安全推送
4. **数据格式**：`{ type: 'stats', room_id: number, data: {...} }`

##### P2-7 频率折线图实现步骤

1. **安装依赖**：`pnpm add echarts echarts-for-react`
2. **组件封装**：`FrequencyChart` 组件封装 ECharts 配置
3. **动态更新**：监听数据变化，实时更新图表
4. **空状态处理**：无数据时显示友好提示

##### P2-8 情感饼图实现步骤

1. **组件封装**：`SentimentPieChart` 组件封装饼图配置
2. **颜色编码**：正面绿色 #22c55e，负面红色 #ef4444，中性灰色 #9ca3af
3. **交互增强**：hover 显示详细统计信息

##### P2-9 关键词词云实现步骤

1. **安装依赖**：`pnpm add echarts-wordcloud`
2. **组件封装**：`KeywordCloud` 组件封装词云配置
3. **数据限制**：只显示 Top 25 关键词，避免渲染性能问题
4. **多彩配色**：随机分配颜色，提升视觉效果

##### P2-10 布局整合步骤

1. **组件拆分**：创建 `RoomHeader`、`DanmakuItem`、`DanmakuContainer`、`AnalysisPanel` 组件
2. **布局重构**：RoomDetail 作为状态管理中心，组合各子组件
3. **数据流优化**：通过 props 传递数据，通过 callbacks 处理事件
4. **样式统一**：使用 TailwindCSS 统一组件样式

---

### 3.4 P3 — 深度分析

**目标**：实现历史弹幕的离线深度分析，提供比实时分析更丰富的数据洞察。

**时间预估**：3 周

| 任务编号 | 任务描述 | 优先级 | 预估工时 | 产出 |
|---------|---------|--------|---------|------|
| P3-1 | 深度分析任务 API（创建/查询/删除） | P3 | 1d | `/api/analysis/*` |
| P3-2 | TF-IDF 关键词提取算法 | P3 | 2d | 全量关键词 |
| P3-3 | 情感时间分布聚合（按分钟粒度） | P3 | 1d | 时序情感 |
| P3-4 | 热点时刻检测算法 | P3 | 2d | 高潮点定位 |
| P3-5 | 用户画像统计（等级、勋章、活跃度） | P3 | 1d | 用户分析 |
| P3-6 | 深度分析引擎整合 `deep.ts` | P3 | 1d | DeepAnalyzer |
| P3-7 | 深度分析前端页面（时间选择 + 结果展示） | P3 | 3d | DeepAnalysis 页 |
| P3-8 | 热点时刻时间轴可视化 | P3 | 1d | HotMomentsTimeline |
| P3-9 | 用户画像图表（饼图 + 排行榜） | P3 | 1d | UserProfileCharts |
| P3-10 | 分析报告导出（JSON/HTML） | P3 | 1d | 报告导出 |

**里程碑 M3 — 深度分析可用**：用户可选择时间范围，生成包含关键词、情感趋势、热点时刻、用户画像的完整分析报告。

---

### 3.5 P4 — 完善与打磨

**目标**：提升稳定性、性能和用户体验。

**时间预估**：2 周

| 任务编号 | 任务描述 | 优先级 | 预估工时 | 产出 |
|---------|---------|--------|---------|------|
| P4-1 | 回放弹幕 XML 解析与导入 | P4 | 2d | 回放导入功能 |
| P4-2 | 回放管理页面（上传 + 列表 + 详情） | P4 | 2d | ReplayManager |
| P4-3 | 自定义词典管理界面 | P4 | 1d | 词典 CRUD |
| P4-4 | 连接异常处理与用户提示 | P4 | 1d | 错误降级 |
| P4-5 | 长时间运行稳定性（内存监控、自动重启） | P4 | 1d | 稳定性保障 |
| P4-6 | UI 细节（加载态、空态、错误态） | P4 | 1d | UI 完善 |
| P4-7 | 高弹幕量场景性能优化 | P4 | 1d | 性能调优 |
| P4-8 | 最终测试与 Bug 修复 | P4 | 1d | 稳定性 |

**里程碑 M4 — 正式发布**：功能完善、性能稳定，可投入日常使用。

---

## 四、里程碑总览

```
  M0               M1               M2               M3               M4
  采集链路打通       实时弹幕墙         MVP 完整         深度分析          正式发布
  ──●───────────────●───────────────●───────────────●───────────────●──→
  第2周末           第5周末           第8周末           第11周末          第13周末
```

| 里程碑 | 时间节点 | 验收标准 |
|--------|---------|---------|
| M0 采集链路打通 | 第 2 周末 | CLI 运行后持续采集弹幕并写入 SQLite |
| M1 实时弹幕墙 | 第 5 周末 | Web 界面可添加直播间、查看实时弹幕滚动 |
| M2 MVP 完成 | 第 8 周末 | 弹幕墙 + 频率 + 情感 + 关键词四维看板可用 |
| M3 深度分析 | 第 11 周末 | 可生成完整深度分析报告 |
| M4 正式发布 | 第 13 周末 | 功能完整、稳定可用 |

---

## 五、资源需求

### 5.1 开发环境

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10+ / macOS 12+ / Linux |
| Python | v3.12+ |
| 包管理器 | pip / uv |
| Node.js | v20.x LTS（仅前端） |
| 代码编辑器 | VS Code |
| 浏览器 | Chrome / Edge 最新版 |
| 网络 | 可访问 B 站 API 和弹幕 WebSocket 服务 |

### 5.2 外部依赖

| 依赖 | 用途 | 风险等级 |
|------|------|---------|
| B 站弹幕 WebSocket 服务 | 弹幕数据源 | 高（非官方协议，可能变更） |
| B 站房间信息 API | 获取 token 和服务器地址 | 中（公开 API，相对稳定） |
| jieba | 中文分词 | 低（纯 Python 实现，无编译依赖） |
| SnowNLP | 中文情感分析 | 低（纯 Python 实现） |
| ECharts | 数据可视化 | 低（成熟开源库） |
| echarts-wordcloud | 词云可视化 | 低（成熟开源库） |

### 5.3 人力投入

| 阶段 | 角色 | 投入比例 | 周期 |
|------|------|---------|------|
| P0 基础采集 | 后端开发 | 100% | 2 周 |
| P1 实时展示 | 后端 + 前端 | 50% + 50% | 3 周 |
| P2 实时分析 | 后端 + 前端 + 算法 | 40% + 40% + 20% | 3 周 |
| P3 深度分析 | 后端 + 前端 + 算法 | 30% + 40% + 30% | 3 周 |
| P4 完善打磨 | 全栈 | 100% | 2 周 |

---

## 六、风险管理

### 6.1 风险矩阵

| 编号 | 风险描述 | 概率 | 影响 | 等级 | 应对措施 |
|------|---------|------|------|------|---------|
| R1 | B 站弹幕协议变更导致采集失效 | 中 | 高 | 🔴 高 | 模块化 parser 设计，协议变更仅需修改解析层；关注社区协议更新动态；加入协议版本兼容逻辑 |
| R2 | B 站风控封禁采集 IP | 低 | 高 | 🟡 中 | 限制单 IP 连接数 ≤ 5；控制心跳间隔合理；必要时加入 cookie/登录态 |
| R3 | Python 依赖版本冲突 / 安装失败 | 低 | 中 | 🟢 低 | 使用 `pip freeze` 锁定版本；推荐使用 `uv` 或 `venv` 虚拟环境隔离；jieba 和 SnowNLP 均为纯 Python 无编译依赖 |
| R4 | 高弹幕量导致前端卡顿 | 中 | 中 | 🟡 中 | 虚拟滚动 + 渲染采样；弹幕墙保留最近 N 条；图表降低更新频率 |
| R5 | 长时间运行内存泄漏 | 中 | 中 | 🟡 中 | 环形缓冲区限制内存；定时重启采集进程；定期 heapdump 分析 |
| R6 | 情感词典覆盖不足导致分析偏差大 | 高 | 低 | 🟢 低 | 支持自定义词典扩充；标明分析结果仅供参考；深度分析预留大模型 API 接口 |
| R7 | 单个开发者时间不足，进度延迟 | 中 | 中 | 🟡 中 | 优先交付 P0-P2 MVP 核心功能；P3/P4 可后续迭代 |
| R8 | echarts-wordcloud 版本兼容问题 | 低 | 低 | 🟢 低 | 锁定 echarts 和 echarts-wordcloud 版本；使用类型断言处理类型定义 |

### 6.2 应急预案

| 场景 | 应急预案 |
|------|---------|
| B 站协议在开发中变更 | 暂停采集模块开发，优先调研新协议；已有数据不受影响，先完成分析模块 |
| 核心依赖不可用 | jieba → pynlpir / thulac；SnowNLP → 自建情感词典方案；websockets → aiohttp WebSocket |
| 前端框架问题 | React 遇到阻塞 → 降级为原生 HTML + ECharts CDN 静态页面 |
| 进度严重滞后 | 砍掉 P3 回放导入和 P4 词典管理，聚焦核心采集+分析+展示 |
| 词云渲染失败 | 降级为关键词列表展示，保留核心功能 |

---

## 七、质量保障

### 7.1 代码规范

| 类别 | 规范 |
|------|------|
| 后端语言 | Python 3.12+，PEP 8 风格 |
| 前端语言 | TypeScript 严格模式（strict: true） |
| 后端风格 | ruff（lint）+ black（format） |
| 前端风格 | ESLint + Prettier（React 规则） |
| 命名规范 | Python: snake_case；TS/React: camelCase + PascalCase |
| 注释规范 | Python: docstring（Google风格）；TS: JSDoc |

### 7.2 测试策略

| 层级 | 范围 | 方法 | 目标 |
|------|------|------|------|
| 单元测试 | 协议解析、分析算法 | 手动验证 + 关键路径自动化 | 核心逻辑正确 |
| 集成测试 | API 端点、数据库操作 | 手动测试 + curl 脚本 | 接口正确性 |
| 端到端测试 | 采集→存储→分析→展示 | 手动操作完整流程 | 链路通畅 |
| 性能测试 | 高弹幕量（500条/秒）场景 | 模拟压测 | 不卡顿不崩溃 |
| 兼容性测试 | B 站不同直播间类型 | 多直播间实测 | 协议兼容 |

### 7.3 验收标准

每个里程碑对应的验收标准见第四章里程碑总览。

---

## 八、交付物清单

| 编号 | 交付物 | 阶段 | 格式 |
|------|--------|------|------|
| D1 | 项目规格文档 | 启动前 | Markdown（docs/spec.md） |
| D2 | 项目计划书 | 启动前 | Markdown（docs/project-plan.md） |
| D3 | B 站弹幕协议解析模块 | P0 | Python 源码 |
| D4 | 弹幕采集 CLI Demo | P0 | 可运行脚本 |
| D5 | 后端 API 服务 | P1 | Python 源码 |
| D6 | 前端 SPA 应用 | P1-P4 | React 源码 + 构建产物 |
| D7 | 情感词典 | P2 | TXT 文件 |
| D8 | 停用词库 | P2 | TXT 文件 |
| D9 | 深度分析模块 | P3 | Python 源码 |
| D10 | 用户使用说明 | P4 | Markdown（docs/usage.md） |

---

## 九、沟通计划

| 节点 | 频率 | 内容 |
|------|------|------|
| 每日站会 | 每天 | 昨日进展、今日计划、阻塞项 |
| 阶段评审 | 每个阶段结束时 | 演示当前阶段产出、确认进入下一阶段 |
| 里程碑评审 | 每个里程碑达成时 | 验收确认、文档更新 |
| 变更管理 | 按需 | 需求变更评估、计划调整 |

---

## 十、附录

### 10.1 关键 API 参考

| API | 方法 | URL | 说明 |
|-----|------|-----|------|
| 房间信息初始化（短号解析） | GET | `https://api.live.bilibili.com/room/v1/Room/room_init` | 将短号转为真实房间号，检查直播状态 |
| 获取弹幕服务器信息 | GET | `https://api.live.bilibili.com/xlive/web-room/v1/index/getDanmuInfo` | 需 WBI 签名 + buvid3 cookie |
| 获取 WBI 密钥 | GET | `https://api.bilibili.com/x/web-interface/nav` | 获取 img_key/sub_key |
| 弹幕 WebSocket | WSS | `wss://{host}:{wss_port}/sub` | 二进制协议，需先认证 |

### 10.2 技术参考

- [B 站直播弹幕协议分析社区文档](https://github.com/lovelyyoshino/Bilibili-Live-API)
- [B 站 WebSocket 弹幕协议说明](https://socialsisteryi.github.io/bilibili-API-collect/docs/live/message_stream.html)
- [jieba 分词 GitHub](https://github.com/fxsjy/jieba)
- [SnowNLP GitHub](https://github.com/isnowfy/snownlp)
- [FastAPI 官方文档](https://fastapi.tiangolo.com/)
- [ECharts 官方文档](https://echarts.apache.org/)
- [echarts-wordcloud GitHub](https://github.com/ecomfe/echarts-wordcloud)

### 10.3 术语表

| 术语 | 说明 |
|------|------|
| DANMU_MSG | B 站弹幕消息类型，CMD=5 |
| proto_ver | B 站 WebSocket 协议版本，决定 body 编码方式 |
| TF-IDF | 词频-逆文档频率，关键词权重算法 |
| 虚拟滚动 | 只渲染可视区域 DOM 节点，优化大量列表渲染性能 |
| WAL 模式 | SQLite 的 Write-Ahead Logging，提升并发写入性能 |
| 滑动窗口 | 基于时间的固定窗口统计方法，只保留窗口内的数据 |
| 最小堆 | 一种数据结构，用于高效维护 Top K 元素 |
| 注册模式 | 通过装饰器或配置注册组件，实现解耦和动态扩展 |